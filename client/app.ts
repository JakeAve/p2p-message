// Temporary placeholder. The prototype UI depended on client/messenger.ts,
// which was removed when the session layer was rebuilt. Plan 4 (04-ui-build.md)
// replaces this module with the real create/share/join/chat views.

document.body.innerHTML =
  "<p>This app is being rebuilt. The chat UI returns in the next release.</p>";

export {};
