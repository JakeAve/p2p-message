# P2P Messaging

Proof of concept for messaging via WebRTC. No messages sent through the server. Both users must be active to send.

## Requirements

- Deno 2+

## Run

```bash
deno install
deno task dev
```